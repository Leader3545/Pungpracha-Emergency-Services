document.addEventListener("DOMContentLoaded", function() {
  AOS.init({
    duration: 1000,
    once: true
  });
});

function loadMoreNews() {
  // ตัวอย่างสำหรับโหลดข่าวเพิ่มเติม (สามารถเพิ่ม AJAX ในอนาคต)
  alert("ฟังก์ชันโหลดข่าวเพิ่มเติมอยู่ระหว่างการพัฒนา");
};
